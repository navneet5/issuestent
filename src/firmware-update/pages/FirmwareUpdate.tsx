import React from 'react'

import { MainTemplate } from 'global'

import { FirmwareUpdateContainer } from '../components'

export const FirmwareUpdate = (props: any) => (
<MainTemplate>
  <FirmwareUpdateContainer />
</MainTemplate>
)
